import React from 'react';
import alertService from '../../services/alertService'; // Adjust path as needed

const AdminSetup = () => {
    // Example usage:
    // alertService.error('Setup Error', 'Failed to complete admin setup.');
    return <div>Admin Setup Page</div>;
};

export default AdminSetup;